# FastSpeech2（DF拡張版）学習条件詳細

## 1. データセット設定

### 1.1 データセット情報
- **データセット名**: LJSpeech-1.1
- **データセットパス**: `./dataset/LJSpeech-1.1`
- **言語**: 英語（English）
- **話者数**: 1名（単一話者）
- **データセットサイズ**: 約13,100個の音声クリップ、総時間約24時間
- **バリデーションセットサイズ**: 512サンプル

### 1.2 データ前処理
- **テキストクリーナー**: `english_cleaners`
- **辞書パス**: `lexicon/librispeech-lexicon.txt`
- **アライメント手法**: Montreal Forced Aligner (MFA) を使用して音素列と音声のアライメントを取得

## 2. 音声前処理パラメータ

### 2.1 音声信号処理
- **サンプリングレート**: 22,050 Hz
- **最大波形値**: 32,768.0
- **量子化**: 16-bit PCM

### 2.2 STFT（Short-Time Fourier Transform）パラメータ
- **フィルタ長（filter_length）**: 1,024
- **ホップ長（hop_length）**: 256サンプル（約11.6 ms）
- **ウィンドウ長（win_length）**: 1,024サンプル

### 2.3 メルスペクトログラムパラメータ
- **メルチャネル数（n_mel_channels）**: 80
- **メル周波数最小値（mel_fmin）**: 0 Hz
- **メル周波数最大値（mel_fmax）**: 8,000 Hz（HiFi-GANボコーダー用）

## 3. 分散特徴量（Variance Features）設定

### 3.1 ピッチ（Pitch）特徴量
- **特徴量レベル**: `phoneme_level`（音素レベル）
- **正規化**: 有効（`normalization: True`）
- **抽出手法**: DIO（Distributed Inline Operation）+ StoneMask（World vocoder）
- **量子化**: 線形量子化（`linear`）
- **量子化ビン数**: 256

### 3.2 エネルギー（Energy）特徴量
- **特徴量レベル**: `phoneme_level`（音素レベル）
- **正規化**: 有効（`normalization: True`）
- **抽出手法**: メルスペクトログラムから計算
- **量子化**: 線形量子化（`linear`）
- **量子化ビン数**: 256

### 3.3 継続時間（Duration）特徴量
- **対数スケール**: 使用（`log(duration + 1)`）
- **アライメント**: MFAから取得

### 3.4 動的特徴量（Dynamic Feature, DF）
- **有効化**: 有効（`enable: True`）
- **特徴量レベル**: `frame_level`（フレームレベル）
- **正規化**: 有効（`normalization: True`）
- **計算手法**: 
  - ケプストラム解析に基づく動的特徴量
  - リフタリング次数（order）: 32
  - 時間幅パラメータ（K）: 2
  - **計算フロー**:
    1. 音声波形から振幅スペクトログラムを取得（STFT）
    2. 対数スペクトログラムを計算: `log(spec)`
    3. 対数スペクトログラムを左右対称に復元
    4. IFFTによりケプストラム（cepstrogram）を計算
    5. リフタリング（order=32）を適用
    6. 線形単回帰によりΔケプストラム（delta cepstrogram）を計算
       - 時間幅: `[-K, K]` = `[-2, 2]`フレーム
    7. Δケプストラムのノルムを計算してDF時系列を取得
       - ノルム計算式: `(20/log(10)) × sqrt(2 × Σ(dCeps[1:])² + dCeps[0]²)`
       - パワー成分を含む（`isPower=True`）
    8. パディング: DFは`2×K`フレーム短くなるため、前後に`K`フレームずつedgeパディング
- **量子化**: 線形量子化（`linear`）
- **量子化ビン数**: 256
- **パディング方法**: edge（端の値で埋める）

## 4. モデルアーキテクチャ

### 4.1 Transformerエンコーダ
- **レイヤー数**: 4層
- **アテンションヘッド数**: 2
- **隠れ次元数**: 256
- **ドロップアウト率**: 0.2
- **畳み込みフィルタサイズ**: 1,024
- **畳み込みカーネルサイズ**: [9, 1]

### 4.2 Transformerデコーダ
- **レイヤー数**: 6層
- **アテンションヘッド数**: 2
- **隠れ次元数**: 256
- **ドロップアウト率**: 0.2
- **畳み込みフィルタサイズ**: 1,024
- **畳み込みカーネルサイズ**: [9, 1]

### 4.3 Variance Predictor（分散予測器）
- **フィルタサイズ**: 256
- **カーネルサイズ**: 3
- **ドロップアウト率**: 0.5
- **予測対象**: Duration, Pitch, Energy, DF（Dynamic Feature）

### 4.4 Post-Net
- **アーキテクチャ**: Tacotron-2スタイルのPost-Net（追加実装）
- **目的**: メルスペクトログラムの後処理

### 4.5 その他の設定
- **最大シーケンス長**: 1,000トークン
- **マルチスピーカー**: 無効（`multi_speaker: False`）
- **モデルパラメータ数**: モデル初期化時に計算（`get_param_num`関数で取得）
  - 計算方法: すべてのパラメータテンソルの要素数の合計（`sum(param.numel() for param in model.parameters())`）

## 5. 学習設定

### 5.1 オプティマイザ
- **オプティマイザ**: Adam
- **ベータ1（β₁）**: 0.9
- **ベータ2（β₂）**: 0.98
- **イプシロン（ε）**: 1.0 × 10⁻⁹
- **重み減衰（weight_decay）**: 0.0（L2正則化なし）

### 5.2 学習率スケジュール
- **初期学習率**: `(encoder_hidden)⁻⁰·⁵ = (256)⁻⁰·⁵ ≈ 0.0625`
- **ウォームアップステップ数**: 4,000ステップ
- **学習率スケール関数**: 
  ```
  lr_scale = min(step⁻⁰·⁵, warmup_steps⁻¹·⁵ × step)
  ```
- **アニーリングステップ**: [300,000, 400,000, 500,000]ステップ
- **アニーリング率**: 0.3（各アニーリングステップで学習率を0.3倍に減少）

### 5.3 バッチ処理
- **バッチサイズ**: 16
- **グループサイズ**: 4（データセット内でのソート用）
- **実効バッチサイズ**: 16 × 4 = 64
- **勾配累積ステップ数**: 1（勾配累積なし）
- **シャッフル**: 有効

### 5.4 勾配クリッピング
- **勾配クリッピング閾値**: 1.0
- **手法**: `nn.utils.clip_grad_norm_`を使用

### 5.5 学習ステップ設定
- **総学習ステップ数**: 900,000ステップ
- **ログ記録間隔**: 100ステップごと
- **音声合成サンプル生成間隔**: 1,000ステップごと
- **バリデーション評価間隔**: 1,000ステップごと
- **チェックポイント保存間隔**: 100,000ステップごと

## 6. 損失関数

### 6.1 損失関数の構成
総損失は以下の6つの損失の合計：

1. **メルスペクトログラム損失（Mel Loss）**
   - 損失関数: L1 Loss（Mean Absolute Error）
   - 予測対象: デコーダ出力のメルスペクトログラム

2. **Post-Netメルスペクトログラム損失（Post-Net Mel Loss）**
   - 損失関数: L1 Loss（Mean Absolute Error）
   - 予測対象: Post-Net出力のメルスペクトログラム

3. **ピッチ損失（Pitch Loss）**
   - 損失関数: MSE Loss（Mean Squared Error）
   - 予測対象: ピッチ特徴量（音素レベル）

4. **エネルギー損失（Energy Loss）**
   - 損失関数: MSE Loss（Mean Squared Error）
   - 予測対象: エネルギー特徴量（音素レベル）

5. **継続時間損失（Duration Loss）**
   - 損失関数: MSE Loss（Mean Squared Error）
   - 予測対象: 対数継続時間（`log(duration + 1)`）

6. **動的特徴量損失（DF Loss）**（本実装の追加要素）
   - 損失関数: MSE Loss（Mean Squared Error）
   - 予測対象: 動的特徴量（フレームレベル）

### 6.2 損失関数の重み
すべての損失項は等しい重み（1.0）で合算される：
```
Total Loss = Mel Loss + Post-Net Mel Loss + Duration Loss + Pitch Loss + Energy Loss + DF Loss
```

### 6.3 マスキング
- **ソースマスク**: 音素レベルの特徴量（ピッチ、エネルギー、継続時間）に適用
- **メルマスク**: フレームレベルの特徴量（メルスペクトログラム、DF）に適用

## 7. ハードウェア・ソフトウェア環境

### 7.1 ハードウェア
- **デバイス**: CUDA対応GPU（利用可能な場合）、CPU（フォールバック）
- **並列化**: `nn.DataParallel`を使用したマルチGPU対応（利用可能な場合）
- **デバイス検出**: `torch.cuda.is_available()`で自動検出

### 7.2 ソフトウェア依存関係
- **PyTorch**: 1.13.1
- **Python**: 3.x系
- **主要ライブラリ**:
  - librosa: 0.7.2（音声処理）
  - pyworld: 0.2.10（ピッチ抽出）
  - numpy: 1.19.0
  - scipy: 1.5.0
  - tensorboard: 2.2.2（ログ記録）
  - tqdm: 4.46.1（プログレスバー）
  - PyYAML: 5.4.1（設定ファイル読み込み）

## 8. ボコーダー設定

### 8.1 ボコーダー
- **モデル**: HiFi-GAN
- **話者設定**: LJSpeech
- **用途**: メルスペクトログラムから音声波形への変換（学習時は評価用、推論時は音声合成用）

## 9. データローダー設定

### 9.1 データセット処理
- **ソート**: 有効（`sort=True`）
- **ドロップラスト**: 有効（`drop_last=True`）
- **コレート関数**: データセット専用の`collate_fn`を使用

## 10. ログ記録・可視化

### 10.1 TensorBoardログ
- **トレーニングログパス**: `./output/log/LJSpeech/train`
- **バリデーションログパス**: `./output/log/LJSpeech/val`
- **記録内容**:
  - 損失値（各損失項と総損失）
  - 合成メルスペクトログラム
  - 再構成音声と合成音声の波形

### 10.2 テキストログ
- **ログファイル**: `log.txt`（トレーニング・バリデーションそれぞれに保存）
- **記録間隔**: 100ステップごと

## 11. チェックポイント・モデル保存

### 11.1 保存パス
- **チェックポイントパス**: `./output/ckpt/LJSpeech`
- **結果保存パス**: `./output/result/LJSpeech`

### 11.2 保存内容
- **モデル状態辞書**: `model.module.state_dict()`
- **オプティマイザ状態辞書**: `optimizer._optimizer.state_dict()`
- **ファイル形式**: `.pth.tar`

## 12. 本実装の特徴（DF拡張版）

### 12.1 Dynamic Feature（DF）の追加
- 標準のFastSpeech2に加えて、動的特徴量（DF）の予測を追加
- DFはケプストラム解析に基づく時系列特徴量
- フレームレベルでの予測と埋め込みを実装

### 12.2 実装ファイル
- **モデル**: `model/fastspeech2_df.py`（FastSpeech2DFクラス）
- **損失関数**: `model/loss_df.py`（FastSpeech2LossDFクラス）
- **データセット**: `dataset_df.py`（DatasetDFクラス）
- **ユーティリティ**: `utils/model_df.py`, `utils/tools_df.py`
- **評価**: `evaluate_df.py`

### 12.3 設定ファイル
- **前処理設定**: `config/LJSpeech/preprocess.yaml`
- **モデル設定**: `config/LJSpeech/model_df.yaml`
- **学習設定**: `config/LJSpeech/train_df.yaml`

## 13. 学習実行コマンド

### 13.1 DF拡張版の学習
```bash
python3 train.py \
  --use_df \
  -p config/LJSpeech/preprocess.yaml \
  -m config/LJSpeech/model_df.yaml \
  -t config/LJSpeech/train_df.yaml \
  --restore_step 0
```

### 13.2 チェックポイントからの再開
```bash
python3 train.py \
  --use_df \
  -p config/LJSpeech/preprocess.yaml \
  -m config/LJSpeech/model_df.yaml \
  -t config/LJSpeech/train_df.yaml \
  --restore_step 100000
```

### 13.3 標準版FastSpeech2の学習（比較用）
```bash
python3 train.py \
  -p config/LJSpeech/preprocess.yaml \
  -m config/LJSpeech/model.yaml \
  -t config/LJSpeech/train.yaml \
  --restore_step 0
```

## 14. 参考文献・ベース実装

- **元論文**: FastSpeech 2: Fast and High-Quality End-to-End Text to Speech (Ren et al., 2020)
- **ベース実装**: [ming024/FastSpeech2](https://github.com/ming024/FastSpeech2)
- **データセット**: [LJSpeech Dataset](https://keithito.com/LJ-Speech-Dataset/)

---

**注意事項**:
- 本ドキュメントは`config/LJSpeech/train_df.yaml`を使用したDF拡張版FastSpeech2の学習条件を記載しています
- 標準版FastSpeech2との主な違いは、DF（Dynamic Feature）の追加とそれに伴う損失関数・モデル構造の変更です
- 実際の学習時間はハードウェア環境に依存します

